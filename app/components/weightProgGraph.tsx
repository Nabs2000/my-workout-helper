import type { User } from "~/types/User";

export default function WeightProgGraph({ user }: { user: User }) {
    return (
        <div>
            <h1>Weight Progress Graph</h1>
        </div>
    );
}